apiVersion: v1
kind: Service
metadata:
  name: {{ include "trabalho-final.fullname" . }}
  labels:
    {{- include "trabalho-final.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "trabalho-final.selectorLabels" . | nindent 4 }}
