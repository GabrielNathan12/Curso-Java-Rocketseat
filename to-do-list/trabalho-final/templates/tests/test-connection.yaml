apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "trabalho-final.fullname" . }}-test-connection"
  labels:
    {{- include "trabalho-final.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "trabalho-final.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
